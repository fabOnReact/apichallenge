<p class="comments"><span>user</span> <%= comment.user.name %> <span>says</span> <%= comment.message%> <span>created the</span> <%= comment.created_at.to_date %> <span>with description</span> <%= comment.description %></p>
<% if current_user.owns?(comment) %>
  <%= link_to "edit", edit_comment_path(comment) %>
  <%= link_to "show", comment_path(comment) %>
<% end %>
